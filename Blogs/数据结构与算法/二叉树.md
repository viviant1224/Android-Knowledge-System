# 二叉树

## 遍历方式

### 前序遍历
递归解法：

```
 public List<Integer> preorderTraversal(TreeNode root) {
        List<Integer> res = new ArrayList<>();
        ergodic(root, res);
        return res;
    }
    
    private void ergodic(TreeNode root, List<Integer> res) {
        if (root == null) {
            return;
        }
        res.add(root.val);
        if (root.left != null) {
            res.add(root.left.val);
        }
        if (root.right != null) {
            res.add(root.right.val);
        }
    }
```

### 中序遍历

同上

### 后序遍历

同上

### 层序遍历
